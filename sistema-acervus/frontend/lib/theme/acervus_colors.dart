import 'package:flutter/material.dart';

class AcervusColors {
  static const primary = Color(0xFF1E3A5F); // Azul escuro (institucional)
  static const secondary = Color(0xFF2F80C2); // Azul médio
  static const accent = Color(0xFFF2994A); // Laranja (bookmark)
  static const background = Color(0xFFF7F9FC); // Fundo claro
  static const surface = Colors.white;

  static const textPrimary = Color(0xFF1F2933);
  static const textSecondary = Color(0xFF6B7280);

  static const success = Color(0xFF2E7D32); // só para status
  static const danger = Color(0xFFD32F2F);
  static const warning = Color(0xFFF59E0B);

  static const border = Color(0xFFE5E7EB);
}
