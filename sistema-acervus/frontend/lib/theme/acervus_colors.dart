import 'package:flutter/material.dart';

/// Tokens de cor do design system Acervus (mockup "Telas Acervus").
class AcervusColors {
  // Marca
  static const primary = Color(0xFF4F46E5); // Índigo principal
  static const primaryDark = Color(0xFF4338CA);
  static const primarySoft = Color(0xFFEEF2FF); // Fundo de item ativo/chips
  static const purple = Color(0xFF6750A4);

  // Feedback
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const danger = Color(0xFFEF4444);

  // Superfícies
  static const background = Color(0xFFF8F9FB);
  static const surface = Colors.white;
  static const border = Color(0xFFE5E7EB);

  // Texto
  static const textPrimary = Color(0xFF111827);
  static const textSecondary = Color(0xFF6B7280);
  static const textMuted = Color(0xFF9CA3AF);

  // Gradiente institucional (login)
  static const gradientStart = Color(0xFF4338CA);
  static const gradientEnd = Color(0xFF7C3AED);

  // Séries de gráficos
  static const List<Color> chart = [
    Color(0xFF4F46E5),
    Color(0xFF6366F1),
    Color(0xFF3B82F6),
    Color(0xFF10B981),
    Color(0xFFF59E0B),
    Color(0xFFEF4444),
    Color(0xFFEC4899),
    Color(0xFF8B5CF6),
    Color(0xFF14B8A6),
    Color(0xFF6750A4),
  ];
}
